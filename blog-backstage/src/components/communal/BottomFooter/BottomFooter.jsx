import React from "react";

import { Layout } from "antd";
const { Footer } = Layout;

const BottomFooter = () => {
    return (
        <Footer
            style={{
                textAlign: "center",
            }}>
            @Skyler
        </Footer>
    );
};

export default BottomFooter;
